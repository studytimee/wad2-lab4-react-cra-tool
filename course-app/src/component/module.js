import React from "react"

const Module = (props) => {
    return (
        <tr>
            <td>{props.description.a}</td>
            <td>{props.description.b}</td>
        </tr>
    );

}

export default Module

